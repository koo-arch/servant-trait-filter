export type Class = {
    id: string;
    name: string;
};

export type Attribute = {
    id: string;
    name: string;
};

export type OrderAlignment = {
    id: string;
    name: string;
};

export type MoralAlignment = {
    id: string;
    name: string;
};

export type Trait = {
    id: string;
    name: string;
};