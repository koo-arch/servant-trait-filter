export type Class = {
    id: number;
    name: string;
};

export type Attribute = {
    id: number;
    name: string;
};

export type OrderAlignment = {
    id: number;
    name: string;
};

export type MoralAlignment = {
    id: number;
    name: string;
};

export type Trait = {
    id: number;
    name: string;
};